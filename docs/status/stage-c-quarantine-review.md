# Stage C 隔离采集、人工复核与提升状态

**日期：** 2026-09-01

**检查点：** `aed484538c69d96e3ffdb4b87770e549410cba6c..e33aa78`

**分支：** `develop`

## 结论

Stage C 的自动闭环已经贯通：真实服务可从终局视觉证据写入无真值隔离区，经本地人工
复核后提升为自包含的 `ReviewedStageCSampleV2`，并在删除原隔离事件和复核记录后继续
独立加载、回放和校验来源侧车。真实天天象棋窗口已成功捕获一个用户控制的两步终局
事件，并按门槛安全拒绝了不唯一候选。用户随后完成合法两步纠正；提升校验又因终局占用
证据与纠正局面不一致而拒绝生成 V2。该结果证明失败路径能保护确认局面，但也暴露了
现有占用观察器未能跟踪黑方马落子的缺陷，因此当前仍不能把本阶段称为现场通过。

## 捕获与占用算法

- 默认后端为窗口级 Windows Graphics Capture（`windows-capture==2.0.1`），本机真实
  帧为 `2313×1385`，窗口候选的逻辑客户区为 `1539×922`，对应 150% DPI。
- 原桌面级 DXGI 可见窗口裁切在该 DPI 环境中取到了错误区域，已从 Stage C 默认路径移除；
  它仍保留为可替换探针后端，不作为当前现场门依据。
- 占用算法版本为 `known-position-template-occupancy-v1`。它只在内存中从用户确认的
  `BoardState` 和固定主题画面提取空位/有子模板特征，不保存完整帧或完整棋盘。
- 初次校准必须让 90 个预测占用与确认局面完全一致，并达到最小模板分离度；否则 90 点
  全部返回低置信并拒绝建立基线。
- 当前限制：只支持标准初始局面或用户提供且确认的 FEN；窗口必须可见、无遮挡；主题在
  会话中固定；尺寸或长宽比发生不安全变化时关闭本次采集并要求重新连接。

## 自动验证

最终代码状态上的命令结果：

- Task 9 聚焦测试：`340 passed, 2 skipped in 66.79s`，墙钟 `68.033s`。
- 全量测试：`573 passed, 2 skipped in 59.87s`，墙钟 `62.833s`。
- Ruff：`All checks passed!`，墙钟 `0.076s`。
- mypy：`Success: no issues found in 74 source files`，墙钟 `1.041s`。
- `git diff --check`：通过，墙钟 `0.115s`。
- 跟踪资产扫描：根部 `.local`、截图、数据库、模型、引擎和可执行二进制发现数为 0。
- 长密钥样式扫描：真实发现数为 0；唯一排除项是专门验证日志脱敏的显式测试夹具，已
  人工核对为虚构字符串。

## 真实窗口冒烟

- 目标：可见、无遮挡、红方在下的标准初始人机局面；所有棋盘操作必须由用户完成。
- 三次基线均成功：每次稳定得到 90 个交点，捕获帧尺寸均为 `2313×1385`。
- 前两次监听分别在 180 秒和 600 秒后安全超时；期间没有棋盘变化，没有隔离事件落盘。
- 第三次长监听建立初始基线后，天天象棋内部重新打开了对局渲染表面；旧 WGC 会话继续
  存活但不再收到新表面帧。新的只读 WGC 会话能够立即读到当前画面，因此旧监听被明确
  终止，没有事件落盘。当前实现要求内部重开对局后断开并重新连接。
- 90 点变化与规则层共同推断并由新基线确认当前两步为 `h2e2, h7e7`，当前 FEN 为
  `rnbakabnr/9/1c2c4/p1p1p1p1p/9/9/P1P1P1P1P/1C2C4/9/RNBAKABNR w`。
- 第四次监听在上述 FEN 建立新基线后，完整捕获了用户控制的 `h0g2, h9g7` 终局。
  决策安全拒绝，耗时 `1026.5658 ms`；程序候选为 `e2g2, e7h7` 和
  `e2g2, e7f7`，因候选不唯一、视觉分数/边距及模板置信不足而没有污染确认局面。
- 隔离事件只包含 4 个交点的小裁片和非隐私数值证据。用户已在本地复核卡选择实际两步
  `h0g2, h9g7`，记录类型为 `legal_move_correction`。
- 提升校验返回 `rejected: final_occupancy_mismatch`，没有生成 V2：交点 7 应为空却被报告
  有子，交点 24 应有子却被报告为空，分别对应黑方马 `h9→g7` 的起点和终点。终局还有
  11 个低于 0.65 的占用置信点，最低置信度为 `0.141555`。
- 未保存完整截图、窗口标题、昵称、头像或账号资料。

因此，Task 9 的真实冒烟以“捕获、人工纠正和安全拒绝链路通过”收尾；它不是识别通过，
也不计入可用于门控统计的已复核 V2 样本。

## V2 自包含与防篡改证据

端到端集成测试真实驱动以下服务链：

`FakeFrameSource → LiveSyncSession → StageCTerminalEventWriter → StageCReviewStore → StageCPromotionService → HumanAiStageCSampleLoader → HumanAiStageCReplayer`

它覆盖直接确认和合法两步纠正两条完整流程。提升后删除隔离根和复核根，V2 仍可独立
加载并回放；分别篡改 `source-event-manifest.json` 或 `review-manifest.json` 都会触发来源
完整性错误。纠正样本回放明确报告 `missed_valid=True`，不会把安全拒绝伪装成正确接受。

## 本地样本计数

截至当前版本：

- 隔离事件：1（accepted 0，rejected 1）。
- 人工复核记录：1（直接确认 0，合法纠正 1，预期拒绝 0，丢弃 0）。
- 已提升 V2：0（直接确认 0，合法纠正 0，安全丢弃 0）。

## 未解决风险与下一门槛

1. 当前占用观察器没有正确跟踪黑方马 `h9→g7`：它把马的起点和终点都保留为移动前
   占用状态；必须先修复并用新的真实事件复验，不能降低提升门槛绕过该问题。
2. 固定主题模板尚未证明能覆盖棋子移动后刚露出的所有棋盘底纹、不同缩放和长期光效。
3. 微信内部替换对局渲染表面时，旧 WGC 会话可能保持存活却停止回调；当前要求用户
   断开并重新连接，后续需要单独批准并实现有界的静默会话刷新策略。
4. 实测决策耗时 `1026.5658 ms`，高于 Stage C 的 P95 目标 `500 ms`；单样本不能形成
   P95 结论，但必须在批量采集前优化或解释主要耗时。
5. 还未收集 30 个独立有效事件和 30 个拒绝事件，无法计算真实误接受、覆盖率和 P95。
6. 当前已提升真实样本仍为 0，任何识别准确率数字都没有统计意义。

Pikafish 实时分析和 DeepSeek 教练继续保持锁定。只有评审后的 30/30 Stage C 盲测达到
零错误接受、有效事件覆盖率至少 80%、六类拒绝场景齐全且 P95 不超过 500 ms，才允许
把最终确认局面送入引擎与教练层。
