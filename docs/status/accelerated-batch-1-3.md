# Accelerated Batch 1.3：真实单步捕获验收

**日期：** 2026-08-30

**分支：** `develop`

**起始检查点：** `c2046f43a9173e1d38a14440dc24ece946f98c2e`

**结论：** 突发采样第一次在真实人机回应场景中保留了用户的单步中间局面，正确第一候选为 `i0h0（车一平二）`，候选外最大变化仅 1.6073，说明人机后续走法没有合并进该样本。但当前 RGB 语义模板把终点棋子判得不够可靠，严格观察器返回 `PAUSED_AMBIGUOUS`，没有更新 `BoardState`。因此真实单步“捕获门”通过，“自动接受门”未通过，暂不进入 Pikafish。

## 1. 安全边界

本批只进行可见窗口真实监听：

- 不点击或控制天天象棋。
- 不注入、不读取微信进程内存。
- 不自动走棋。
- 只在初级人机练习中验证。
- 默认不保存完整截图。
- 严格语义间距和置信度门没有降低。

## 2. 局面恢复

最初从 Batch 1.1 的两步后局面启动。第一次用户走棋发生在监听超时后，因此没有冒充成自动识别结果。通过 Git 忽略目录中的临时诊断覆盖图和规则层校验，恢复出：

1. `h0g2（马二进三）`
2. `i9h9（车9平8）`

四步后局面：

```text
rnbakabr1/9/1c4nc1/p1p1p1p1p/9/9/P1P1P1P1P/1C2C1N2/9/RNBAKAB1R w
```

`position_id`：`09f97d7d1490f74ded87550127be29f9`

## 3. 真实单步结果

探针从四步后局面建立真实基线：

| 指标 | 结果 |
|---|---:|
| 捕获尺寸 | 2313×1385 |
| 交点 | 90 |
| 主题模板 | 15 类 |
| 捕获 / 稳态 | 20 / 2 FPS |
| 静默窗 | 100ms |
| 基线 `position_id` | `09f97d7d1490f74ded87550127be29f9` |

用户随后走 `i0h0（车一平二）`，人机正常回应。探针在回应合并前返回：

| 指标 | 结果 |
|---|---:|
| 状态 | `PAUSED_AMBIGUOUS` |
| 第一候选 | `i0h0` |
| 起点帧差 | 14.1714 |
| 终点帧差 | 27.7439 |
| 候选外最大变化 | 1.6073 |
| 候选分数 | 12.5641 |
| 语义距离 | 0.1349 |
| 语义间距 | -0.0261 |
| 语义置信度 | 0.0362 |
| 返回走法 / 新局面 | 无 / 无 |

候选外变化低于既有 3.0 上限，且第一候选与用户实际走法一致，证明 Batch 1.2 的突发采样解决了“两步合并”问题。拒绝原因完全落在终点语义特征：移动后的红车受到交点背景、高亮和阴影影响，虽然绝对语义距离没有超过 0.18，但组间距为负、置信度远低于 0.8。

系统保持四步后 FEN 和原 `position_id`，没有静默污染确认局面。

## 4. 监听结束后的人工恢复

探针安全暂停并退出后，用户继续走棋。后续步骤不属于自动监听结果，只通过当前稳定画面和规则层恢复：

1. `b9c7（马2进3）`
2. `h0h6（车二进六）`
3. `c6c5（卒3进1）`

当前八步后局面：

```text
r1bakabr1/9/1cn3nc1/p3p1pRp/2p6/9/P1P1P1P1P/1C2C1N2/9/RNBAKAB2 w
```

`position_id`：`663c4fda5f01d7e3beb4e6b98e0fcfc3`

第三次监听从该局面成功建立基线，但用户没有继续走棋，随后人工停止；不计为新的单步样本。

## 5. 隐私与资料

- 真实监听没有保存完整截图。
- 为恢复超时后已变化的棋盘，显式诊断覆盖图仅位于 Git 忽略目录：
  - `.local/accelerated-batch-1-3-current/`
  - `.local/accelerated-batch-1-3-after-rook/`
- 诊断文件不进入 Git，不包含 API Key 或数据库。
- 用户原有未提交计划文档保持原样。

## 6. 质量门

| 检查 | 结果 |
|---|---|
| 全量 pytest | 155 passed |
| Ruff | All checks passed |
| mypy | 26 source files, no issues |
| `git diff --check` | 通过 |
| `.local` 忽略验证 | 两个诊断目录均被 `.gitignore` 排除 |

## 7. 下一步门

暂不进入 Pikafish。下一批应先增加显式启用、默认关闭的“仅保存候选起点和终点小裁片”诊断模式，收集多个真实棋子移动到不同背景交点的样本。随后比较抗背景和高亮的特征表示，并继续使用预先锁定的 0.02 语义间距和 0.8 置信度门；不得依据单个样本降低门槛。

至少 3～5 个不同棋子或不同落点的真实单步在严格门下被唯一接受后，才进入 Pikafish 最小分析闭环。
