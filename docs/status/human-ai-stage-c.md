# 人机两步同步 Stage C 状态

**日期：** 2026-09-01

**分支：** `develop`

**代码检查点：** `eb1f89e152141f35d5d086ef3836fd888d69e3cf`

**状态：** 采集、回放和冻结门基础设施完成；真实数据门未通过，实时引擎与教练保持锁定

## 结论

人机模式已经具备独立的两步候选决策门、隐私安全的小块证据、确定性回放、冻结清单、
发布指标门和一次一事件的显式采集器。正常同步会话默认不分配或保存诊断裁切；只有显式
开启 Stage C 采集时，才会在终局事件中保留 1–4 个 48×48 BGRA 交点前后小块和 90 个
局部差异。完整窗口画面不会进入样本或 Git。

自动门全部通过，但真实冻结数据集当前为零，因此这不是 Stage C 通过证明。实时视觉局面
不得自动触发 Pikafish 或 DeepSeek；手工 FEN 分析仍可独立使用。

## 已完成能力

- `STRICT_SINGLE` 与 `HUMAN_VS_AI` 使用同一共享、版本化的决策规则，严格单步行为保持兼容。
- 人机模式可把唯一合法的“用户一步 + 人机一步”作为一个原子事件提交；任一规则提交、
  候选最终局面或 `position_id` 校验失败都会清空走法并暂停。
- Stage C 样本只保存匿名会话/样本 ID、规则证据、版本、捕获上下文和最多四个交点小块。
- 记录器限制容量、保留期、路径和重复 ID；输出只能位于批准的 `.local` 根目录。
- 冻结清单固定样本相对路径、SHA-256、特征版本和完整阈值配置，改动后拒绝评估。
- 发布门要求 30 个独立有效会话、30 个拒绝事件、六类拒绝场景、零错误接受、覆盖率至少
  80%，并且决策延迟 P95 不超过 500 ms。
- 采集器要求显式 FEN、四角、模式、场景和事前标签；超时、窗口关闭、尺寸变化或内部提交
  错误均不写样本。
- 高 DPI 下目录客户区尺寸可以不同于第一张真实捕获帧；同一次采集在首帧之后发生任何尺寸
  变化都会失败关闭。

## 自动验证

在代码检查点提交前完成了以下验证：

```text
相关同步、诊断和脚本测试：206 passed in 25.29s
全量 pytest：435 passed in 28.28s
Ruff：passed
mypy src：passed（67 个源文件）
git diff --check：passed
```

测试覆盖原子接受、安全拒绝、默认关闭诊断、裁切所有权、真实捕获时间戳延迟、DPI 缩放
首帧、预基线和基线后 resize、窗口关闭、超时、重复样本 ID、`.local` 路径约束以及内部提交
错误不得伪装成冻结门拒绝样本。

## 真实只读冒烟

本轮所有棋子操作均由用户完成；程序没有点击、激活、移动、缩放或最小化目标窗口。

### 实际走法和局面证据

用户在标准初始局面走 `h2e2`（炮二平五），初级人机回复 `h7e7`（炮8平5）。随后通过
Windows Graphics Capture 只在内存中检测圆形棋子占位，得到恰好 32 个占用交点：红炮从
70 移到 67，黑炮从 25 移到 22，其余 28 个占用交点保持不变。规则层投影得到：

```text
FEN: rnbakabnr/9/1c2c4/p1p1p1p1p/9/9/P1P1P1P1P/1C2C4/9/RNBAKABNR w
position_id: d4c03fbf35e2cedd0dff78f5897df228
```

没有把本轮当前窗口完整截图写入磁盘；比较时只读取用户已经主动上传的初始局面图片，当前
捕获帧仅存在于内存。

### 观察器结果

第一次监听使用产品当前的 DXGI Desktop Duplication 可见窗口后端，帧尺寸为
2309×1383，90 个交点可以生成。用户为了读取 Codex 指令切回本应用后，Codex 覆盖了天天
象棋；该后端建立了接近白屏的基线。用户再切回棋盘并完成两步时，观察器看到的是“遮挡层
→完整棋盘”的上下文变化，返回：

```text
PAUSED_AMBIGUOUS
reasons: expected_change, outside_change, candidate_score, candidate_margin,
         template_margin, template_confidence
accepted moves: 0
confirmed position_id: 132bdaf223100c4bd42ae8b81f0fb96c
```

因此本轮是真实的**安全拒绝**，不是两步识别通过；最后确认局面没有被错误污染。

作为根因对照，临时只读 WGC 后端在 Codex 覆盖目标窗口时仍取得 2313×1385 的真实目标
内容，建立上述 `d4c03f…` 局面基线，并连续等待 180 秒无误报、无局面变化。这个结果证明
此前拒绝来自违反“可见、无遮挡”前提，而不是无操作时随机接受。WGC 遮挡捕获尚未成为
第一版产品保证，不能替代冻结数据门。

## 隐私与仓库检查

- Git 跟踪文件中没有 `.env`、数据库、PNG、ONNX、引擎/模型二进制或 `.local` 内容。
- 没有新增真实 API Key 或长密钥样式字面量；测试只使用显式测试占位值。
- 用户自有的未跟踪文件
  `docs/superpowers/plans/2026-08-28-remaining-execution-plan.md` 保持原样且不加入提交。
- 本轮没有生成里程碑标签。

## 冻结数据门现状

```text
真实 Stage C 样本：0
有效两步样本：0 / 30 个独立会话
拒绝样本：0 / 30
冻结清单：0
正式发布报告：不存在
```

未持久化的冒烟结果不能冒充冻结样本，也不能计入覆盖率、错误接受率或延迟指标。

## 遗留风险和下一步

1. 真实采集时必须让天天象棋保持可见、无遮挡，或另行设计并验证 WGC 后端的更新、关闭、
   最小化和动态采样语义；不能把本轮临时对照当成产品支持承诺。
2. 当前四角会随天天象棋内部缩放变化；本轮上传图片对应的新交点四角约为
   `0.3045,0.1316;0.6925,0.1316;0.6925,0.8604;0.3045,0.8604`。第一版仍需每次缩放后
   人工确认，不得静默沿用旧标定。
3. 有效样本要求在事件前给出两步真值，而人机回复通常不可预知。冻结采集应使用可复现的
   练习/复盘序列，或先补充独立的事后人工标注与双人复核流程；未经批准不能降低标签门槛。
4. 先在与冻结集严格分离的开发样本上改进特征或阈值，再冻结 30 个独立有效会话和 30 个
   拒绝事件；冻结集不能用于调参。
5. 只有正式门达到零错误接受、至少 80% 覆盖率和 500 ms P95 后，才允许把确认局面自动
   送入 Pikafish 和 DeepSeek。之后再继续 SQLite 复盘、30 分钟生命周期、Windows 目录版
   打包、许可证审计和十盘最终验收。
